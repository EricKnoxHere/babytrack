# Copier ce fichier en .env et remplir les valeurs

# Anthropic (obligatoire pour l'analyse IA)
ANTHROPIC_API_KEY=sk-ant-...

# Base de données SQLite (défaut : data/babytrack.db)
# DATABASE_URL=data/babytrack.db

# Dossier des guides médicaux (défaut : data/docs)
# DOCS_DIR=data/docs

# Dossier de l'index vectoriel (défaut : data/index)
# INDEX_DIR=data/index

# Modèle Claude (défaut : claude-3-haiku-20240307)
# CLAUDE_MODEL=claude-3-haiku-20240307

# URL de l'API (pour l'UI Streamlit, défaut : http://localhost:8000)
# BABYTRACK_API_URL=http://localhost:8000
